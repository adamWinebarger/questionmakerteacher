import 'package:flutter/material.dart';

class BasicScreenTestWidget extends StatelessWidget {
  const BasicScreenTestWidget({super.key});

  @override
  Widget build(context) {
    return const Center(
      child: Text("Banana"),
    );
  }

}